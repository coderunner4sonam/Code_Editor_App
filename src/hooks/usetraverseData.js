import explorerData from "../component/Explorer"

export default function usetraverseData() {
   return function insert(explorerData, name, isFolder, id) {
    if(explorerData.id === id && explorerData.this) {}
   }    
}





